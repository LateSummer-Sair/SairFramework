---
name: 批量转格式
description: 批量图片格式转换（实现已从 Java 剥离到本技能文件夹）
channels: console,execs
permission: ANY
tool: batchconvert
returns: 转换清单与总数（含失败原因）
airun:
  description: 批量图片格式转换（dir=/path from=EXT to=EXT）
  params:
    dir:
      type: string
      description: 目标目录
      required: true
      example: C:/pics
    from:
      type: string
      description: 源格式扩展名
      required: true
      example: png
    to:
      type: string
      description: 目标格式扩展名
      required: true
      example: jpg
---
# 批量转格式（batchconvert）

原硬编码实现：`TagHandlers.executeBatchConvert()`。行为一致：用 `javax.imageio.ImageIO`
逐个读取再写出；读不出（非图片）或没有对应 writer 时逐条报明原因，不会中断整批。
