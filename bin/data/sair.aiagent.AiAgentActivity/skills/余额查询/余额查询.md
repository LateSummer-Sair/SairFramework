---
name: 余额查询
description: 查询 DeepSeek 账户余额（实现已从 Java 剥离到本技能文件夹）
channels: execq,execs,console
permission: ANY
tool: balance
returns: 账户余额文本
airun:
  description: 查询 DeepSeek 账户余额
  params: {}
---
# 余额查询（balance）

原硬编码实现：`AgentActionHandler.executeBalance()` → `DeepSeekClient.queryBalance()`。

客户端从 Java 的 `DeepSeekClient.getInstance()` 取（插件启动时登记的那一份），
不新建实例 —— 避免技能自己再起一个客户端、各持连接与统计。

未初始化时回 `[balance] DeepSeekClient未初始化`，与原实现一致。
